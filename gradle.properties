# Gradle daemon 堆内存（R8 跑在 daemon 内，这是关键）
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8
# 限制并行 worker，避免多 ABI 同时跑 R8 争抢内存
org.gradle.workers.max=2
# R8 内部串行执行，减少峰值内存
android.r8.maxWorkers=1
